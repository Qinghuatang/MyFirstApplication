<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyFirstApplication" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/DeepSkyBlue</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyFirstApplication" parent="Base.Theme.MyFirstApplication" />


    <!-- 自定义标题栏 parent="android:Theme" 这个属性必须写 -->
    <style name="Theme.NewTask" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="android:windowContentOverlay">@null</item>

        <!-- 设置高度，和 mytitlebar.xml中保持一致 -->
        <item name="android:windowTitleSize">50dp</item>
        <!-- 设置内填充为0 使自定义标题填充整个标题栏，否则左右两边有空隙 -->
        <item name="android:padding">0dp</item>

        <!--    自定义主题需要将一下三项设置为false    -->
        <item name="android:windowNoTitle">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowActionBarOverlay">false</item>
    </style>
</resources>
